# Changelog — one-rulebook

Newest on top, dates DD.M.YYYY. Entries after 0.1.0 are generated by the export tool from git —
what each export added, changed and removed — and the source commit is in the commit message.

## 0.1.0 — 20.9.2026

Public export of the private configuration repo (in daily use since 21.5.2026), scoped to the
collaboration of the agents: the rulebook, thirteen hooks with their fixture suite, the review and
fix lanes, the Claude ↔ Codex sync with templates and `local.env`, the quota instruments and
statusline, the process skills, two profiles on one machine, `install.sh`.
